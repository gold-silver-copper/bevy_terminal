[workspace]

[package]
name = "terminal-audit-workloads"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
bevy_terminal = { path = "../../crates/bevy_terminal", features = ["timings"] }
bevy = { version = "=0.19.1", default-features = false, features = ["system_font_discovery"] }
